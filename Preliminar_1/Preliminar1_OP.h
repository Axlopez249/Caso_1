//Axel López Vega
//Lista sencilla doblemente enlazada

//Operaciones a realizar para hacer uso de las 4 funciones principales

tomar5(Datos lista_info);
//Para tomar la prioridad busca nodo por nodo con los datos seleccionados
tomarLista();
//Retorna todos los elementos de la lista. usa la funcion de mostrar nodo siguiente en un ciclo
eliminar_con_info(Datos lista_info);
//Elimina y reacomoda los nodos que tienen esa información. Usa mostrar_nodo_siguiente y eliminar_nodo
subir_bajar(Dato dato, int prioridad);
//sube la prioridad. Busca el dato, obtiene la posi del nodo, Busca el key del nodo de esa prioridad y se ubica atrás de él
//Luego el que estaba ahí se puede eliminar y ponerlo en otro lado o reacomodarlo conectando el que tengo con el que tenía con el siguiente y el anterior
 
